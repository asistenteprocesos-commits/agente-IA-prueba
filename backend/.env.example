# ════════════════════════════════════════════════════════════════════════
# .env.example — Agente BPMS v1.2
# Copia este archivo como  backend/.env  y rellena tus API keys
# ════════════════════════════════════════════════════════════════════════

APP_NAME=Agente BPMS - Experto en Procesos
APP_VERSION=1.2.0
APP_ENV=local
API_PREFIX=/api
FRONTEND_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173","http://localhost:5174","http://127.0.0.1:5174"]

# ── Base de datos ─────────────────────────────────────────────────────
DATABASE_URL=sqlite:///./storage/app.db
DOCUMENT_STORAGE_DIR=storage/documents
AGENT_TRAINING_DIR=docs/agent-training
OBSIDIAN_VAULT_DIR=storage/obsidian-bpm-vault
VECTOR_STORE_DIR=storage/vector_store

# ── Gemini (Google AI Studio) ─────────────────────────────────────────
# Obtén tu clave GRATIS en: https://aistudio.google.com/apikey
GEMINI_API_KEY=tu_gemini_api_key_aqui
GEMINI_MODEL=gemini-2.5-pro-latest
GEMINI_FLASH_MODEL=gemini-2.5-flash-latest

# ── Deepseek API (casi gratis) ────────────────────────────────────────
# Obtén tu clave en: https://platform.deepseek.com
DEEPSEEK_API_KEY=tu_deepseek_api_key_aqui
DEEPSEEK_MODEL=deepseek-chat

# ── Groq (gratis, muy rápido) ─────────────────────────────────────────
# Obtén tu clave GRATIS en: https://console.groq.com
GROQ_API_KEY=tu_groq_api_key_aqui
GROQ_MODEL=meta-llama/llama-4-scout

# ── Ollama (LLM Local — instalar en la PC de pruebas) ────────────────
# NO instalar aquí. Ejecutar pull-local-llm-models.cmd en la otra PC
LOCAL_LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_REASONING_MODEL=deepseek-r1:32b
OLLAMA_REASONING_MODEL_UPGRADE=deepseek-r1:32b
OLLAMA_CODER_MODEL=deepseek-coder-v2
OLLAMA_FAST_MODEL=qwen2.5-coder:7b
OLLAMA_EMBEDDING_MODEL=qwen3-embedding:0.6b
OLLAMA_EMBEDDING_MODEL_UPGRADE=qwen3-embedding:4b

# ── RAG / Embeddings ──────────────────────────────────────────────────
RAG_TOP_K=5
RAG_CHUNK_SIZE=1200
RAG_CHUNK_OVERLAP=160

# ── Chat ──────────────────────────────────────────────────────────────
CHAT_MAX_HISTORY=20
CHAT_MAX_RAG_FRAGMENTS=5
